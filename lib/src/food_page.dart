import 'package:flutter/material.dart';

class FoodQuiz extends StatelessWidget {
  const FoodQuiz({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
